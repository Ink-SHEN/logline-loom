# 2026-09-11

## 对账守护自然收工 + 粗剪 v1 出炉

- 守护 HTEPU6（09-10 20:37 起，10h）跑到 06:37 deadline 后于 10:22 正常退出；
  日志显示 00:34 → 01:04 → 10:22 之间机器休眠，两次 `history 扫描失败：RemoteDisconnected` 属休眠唤醒后的正常抖动，无影响。
- 09-11 10:23 复核：`LOOM/shots` **27/27** 目录齐全且都有 video.mp4；`剧本\reruns\` 37 个返修片。
- **Spark ComfyUI 已不可达**（`http://127.0.0.1:8188/queue` RemoteDisconnected，natapp 隧道断）→ 批跑通道关闭，如需再生成要先恢复隧道。

## 粗剪 v1（27 镜顺序硬切）

- 脚本：`剧本\roughcut.py`（单次 ffmpeg filter_complex，27 路输入）
- 产线：`LOOM\cuts\LOOM_roughcut_v1.mp4` + 同名 `_timeline.csv`
- 规格：1920×1080 / 24fps / h264 crf18 / yuv420p，219.5 s，151 MB
- 混剪对齐做法：Hero 镜 1984×1120 与常规 1280×736 都是非标准 16:9，
  用 `scale=if(gt(a,16/9),1920,-2):if(gt(a,16/9),-2,1080)` + `pad=1920:1080:(ow-iw)/2:(oh-ih)/2`，避免拉伸变形
- 素材本身无声（H3 只出视频），当前为无音轨版；项目内也找不到任何 mp3/wav 音频资产
- 复用：换镜头版本只需替换 `shots/S0XX_c01/video.mp4` 后重跑 `roughcut.py`；换 take 可从 `reruns\` 挑

# 会议内容

**会议时间**：2026-09-04（周五）晚
**距提交截止**：09-14 22:00，还剩 **10 天**
**本文覆盖**：09-05（六）→ 09-09（三）这 5 天，其中 **09-08（二）是预设的弃车保帅决策日**
**参会**：三人全到。这场会开完必须拿到 10 个决定（见文末清单），少一个 Day 1 就会堵

> 配套文件都在本机项目目录下，**没有传到 DGX Spark 上**：
> - `contracts/film_agent_contracts.json` — 7 道接口契约（JSON Schema）
> - `contracts/examples.json` — 每道契约一份可抄的示例产物
> - `contracts/validate_contract.py` — 校验器
> - `contracts/negative_test.py` — 反向测试（证明校验器抓得住错）

---

## 一、先对齐现状：哪些是真的做完了

说清楚这个是为了会上不重复讨论已经解决的事。

### 已经落地、且经过实测验证的

| 项目 | 状态 | 证据 |
|---|---|---|
| DGX Spark + MiniMax-H3 + ComfyUI 部署 | ✅ 完成 | 冒烟测试通过，ComfyUI 0.34.0 |
| 模型权重 | ✅ 齐备 | FL2VA / Ref2VA 两套 + 两份 turbo LoRA 都在节点上 |
| 三份 API 工作流导出 T2V,I2V,R2V | ✅ 完成 | `workflow_api_{t2v,i2v,r2v}.json` 在节点 `~/minimax-h3-dgx-spark/ComfyUI/user/default/` |
| 工作流逐节点预检 | ✅ 0 问题 | 用 `/api/object_info` 比对了每个节点的必填输入、链接指向、枚举取值 |
| 节点 ID 映射表 | ✅ 落盘 | `node_id_map.json`，含 13 组合法取值枚举 + 15 条警告 |
| 映射表与工作流一致性 | ✅ 88 项全 OK | `verify_map.py` 输出 `bad = 0` |
| 画幅统一 16:9 | ✅ 完成 | 三份都是 `16:9 (Widescreen)`，09-04 改完并回传节点验证 |
| 接口契约 v1.0 | ✅ 已写好并测过 | 7 份示例全通过结构校验；8 种真实错误 8/8 被抓住 |

导出过程中修掉的 4 类真实缺陷（**已修，不用再管，但要知道为什么以后不能直接信模板**）：

1. I2V 里有一对悬空节点，其中一个缺必填输入 → ComfyUI 会校验图里**每一个**节点，包括输出没人用的，所以原样提交必失败
2. 三份的图片输入都指向节点上不存在的文件 → 会报 `Value not in list: image`
3. 步数那两个节点的键名是 `value` 不是 `steps`，按 `steps` 写会全部落空
4. I2V 画幅原为 1:1，与另两份不一致

### 还没做的（这就是接下来 5 天的内容）

- ❌ **三份工作流各自的出片冒烟测试**（真跑一次生成，用 ffprobe 确认有 24fps 视频流 + 32kHz 双声道音频流）—— 这项**卡着 09.08 闸门**，必须 Day 1 做
- ❌ 选题与片约
- ❌ 仓库、目录规范、GPU 排班、任务登记表
- ❌ 七个 Agent 的实现（骨架有了，但没对齐契约）
- ❌ 创空间部署
- ❌ 报名与团队规则的官方确认（这个**今天就该去答疑群问**，别等）

---

## 二、硬约束：这些不能商量，排期必须绕着它们走

会上有人提"我们并行多开几个任务快点"时，把这一节念给他听。

**GPU 与内存**
- 只有一台机器，128GB 统一内存（CPU/GPU 共享，`nvidia-smi` 看不到独立显存，要看 `free -h`）
- ComfyUI 是**单例**，`/queue` 是 **FIFO 串行**，不存在并行生成
- **FL2VA 与 Ref2VA 不能同时常驻**，切换要重载 21GB+ 权重 → **必须按工作流类型分批，不能按人分批**
- 生成期间任何人不许在这台机器上跑别的模型或推理服务

**模型能力边界（MiniMax-H3）**

- 单次生成 **4–15 秒**
- 标称 768p / 24 FPS，**原生输出 32kHz 立体声音频**（声音是它的强项，值得用来叙事）
- **没有 negative_prompt，也没有 CFG**（用的是 `BasicGuider` 不是 `CFGGuider`），别在这两项上浪费时间
- 时长填的是**秒**（浮点），下游节点自动换算成帧并对齐到 17 的倍数（5 秒 → 124 帧）。**禁止直接写帧数**
- 2K 需要未发布的 H3-Regenerate-2K，别指望

**素材**

- `LoadImage` 只能引用**节点上真实存在**的文件。素材必须先 `POST /upload/image` 传上去，再重查 `object_info` 确认它出现在枚举里，才能写进工作流

**访问方式**
- ComfyUI **完全没有任何鉴权**。保持 `--listen 127.0.0.1`，三人各自开 SSH 隧道 `ssh -L 8188:127.0.0.1:8188`
- 云节点虽有公网入口，但**不许把 8188 直接暴露**（等于任何人都能白用我们的 GPU、能读节点上的文件）

**比赛规则**
- 主赛道：1–5 分钟原创科幻短片，B站/小红书/抖音链接 + 官网上传原文件（**≤600MB**）+ 魔搭开发者实践创作手记，**视频须标注 AI 生成识别标志**
- Agent 赛道：必须**部署到魔搭创空间**并提交作品链接
- 红线：不得出现《黑客帝国》《银翼杀手2049》《2001太空漫游》《星际穿越》等影片的角色、台词、造型、剧照与片名
- **Qoder Credits 必须注册国际版才发**，三人都要注册，报名表里的 Qoder 邮箱须为国际版账号

---

## 三、五天日程

每天的产出都是**契约文件**（`c01`–`c07`），不是口头进度。当天结束时对应的 JSON 必须进 git 并通过校验器 —— 这是我们唯一的客观进度指标，不靠"我差不多做完了"。

### Day 1 · 09-05（六）打地基

| 谁 | 做什么 | 当天产出 |
|---|---|---|
| **全员上午** | 把契约 v1.0 过一遍，逐条确认能接受，然后冻结进 git | `contracts/` 进仓库，打 tag `contracts-v1.0` |
| **A** | 定选题，写片约 | `c01_brief`（**人工关口：全组批准**） |
| **B** | **三份工作流各跑一次 5 秒冒烟测试**；记录真实单镜头耗时 | 三个 mp4 + ffprobe 输出 + `c05_gen_result` ×3 |
| **B** | 建仓库、目录结构、`.gitignore`、任务登记表 | 仓库可 clone，README 有复现步骤 |
| **C** | 写 GPU 排班表 + 生成 Agent 的骨架对齐 `c04`/`c05` | `docs/gpu_schedule.md` |

**Day 1 结束必须拿到的一个数字：单镜头真实生成耗时。** 后面所有排期都靠它算，现在谁都不知道（模板没跑过）。有了它就能算：

```
总 GPU 时间 ≈ 镜头数 × 每镜头候选数 × 单镜头耗时 × (1 + 重试率)
```

如果算出来超过可用时间，**唯一的杠杆是 turbo LoRA**：T2V/I2V 从 20 步降到 8 步（约 2.5 倍），R2V 从 20 步降到 4 步（约 5 倍）。打开方式是把 `turbo_enabled` 那个布尔节点改成 `true`，`ComfySwitchNode` 会自动切模型和步数。这同时也是「技术创新与模型工程 25 分」里推理优化的明面得分点。

**冒烟测试顺序有讲究**：T2V → I2V 连着跑（都用 FL2VA，权重不用换），R2V 最后跑（要换 Ref2VA）。

### Day 2 · 09-06（日）创意线 + 生成线同时开工

| 谁 | 做什么 | 当天产出 |
|---|---|---|
| **A** | 剧本（可让编剧 Agent 出初稿，人改）→ **剧本确认关口** | `c02_screenplay`，`gate.status = approved` |
| **C** | 分镜：把剧本切成 4–8 秒的独立镜头，逐个标 T2V/I2V/R2V | `c03_shotlist`，含 `batch_plan` |
| **B** | 生成 Agent 打通 ComfyUI API：读 `c04` → 写节点 → `POST /prompt` → 轮询 `/history` → 落盘 → ffprobe → 写 `c05` | 能用一份 `c04` 真出一个镜头 |
| **A+C** | 视觉一致性资产：主角定妆图、场景参考图（R2V 参考集） | 素材上传到节点，记录 license |

**这一天最容易出事的地方**：分镜标 `workflow_type` 的时候，要意识到每个 R2V 镜头都意味着一次权重重载。所以**能用 T2V/I2V 表达的镜头就别用 R2V**，R2V 只留给真正需要角色/场景一致性的关键镜头。这个取舍 Day 2 就要做，不要等到发现 GPU 不够用。

### Day 3 · 09-07（一）闭环

| 谁 | 做什么 | 当天产出 |
|---|---|---|
| **C** | 质检 Agent：ffprobe 硬指标（时长/帧率/分辨率/音频采样率与声道）+ 提示词遵循度打分 → 写 `c06` | 能对一个镜头出 `pass`/`fail` + 可执行的修改建议 |
| **C** | 重试闭环：`c06.route_to = retry` 时自动生成新的 `c04`（换种子为主） | 一个镜头跑完 生成→质检→重试→再质检 |
| **B** | 提示词 Agent：`c03` 的中文画面/声音描述 → 英文提示词，带 `[0s-2s]` 时间码，I2V/R2V 带 `<Picture N>` | `c04_gen_request` |
| **A** | **端到端串一次**：一句话输入 → 编剧 → 分镜 → 提示词 → 生成 → 质检 → 出一个合格镜头 | **这就是 09.08 闸门的判定材料** |
| **B** | 开始批量生成 FL2VA 批次（T2V+I2V 镜头），每镜头 3 候选 | 任务登记表开始有真实数据 |

### Day 4 · 09-08（二）★ 时间闸

**上午**：A 把端到端再复跑一次，收集证据（跑通的镜头文件 + `meta.json` + 日志）。

**中午 30 分钟：闸门决策会，必须书面记录。**

判定标准只有一条，不含糊：

> **能不能从一句话输入，经编剧 → 分镜 → 提示词 → ComfyUI 生成 → 质检，产出一个 4–8 秒、ffprobe 验证含 24FPS 视频流 + 32kHz 双声道音频流的镜头文件，且 meta.json 完整？**

- **通过** → 按原计划双赛道，Agent 承担正式短片生产
- **不通过** → **立即转手工生产线保主赛道**：
  - 短片改为手工在 ComfyUI 完成（Day 1 的冒烟测试已经验证过这条路能走通，可直接复用）
  - Agent 降级为「部分自动化 + 完整架构文档」，**仍然提交 Agent 赛道**
  - 降级为什么还能得分：架构与工程实现占 30 分（Agent 分工 7%、调度与上下文管理 8%、反馈闭环与可观测性 6%、云端本地协同 5%、合规机制 4%），复现开源部署占 10 分。而**自动化程度只占 4%、稳定性完成率只占 3%**。评分表根本没要求 100% 自动化

**决策要写下来的三样**：判定证据、结论、后续分工调整。这份记录本身就是创作手记「失败与修正」章节的核心材料，也是「反馈闭环」的真实证据 —— 就算走了降级路线，它也在给我们挣分。

**下午**：按决策分叉执行。

> 为什么定在 09-08：这天距截止还有 6 天。如果拖到 09-12 才发现 Agent 跑不通，手工重做也来不及了。主赛道那 100 分是确定收益，不该被 Agent 赌掉。

### Day 5 · 09-09（三）批量生产

- **通过路线**：FL2VA 批次全部镜头跑完（多候选）；单独安排一个时段换 Ref2VA 跑 R2V 批次；质检 + 人工筛选同步进行
- **降级路线**：手工在 ComfyUI 出全片镜头，Agent 侧转为补架构文档与可观测性

### 之后（09-10 → 09-14，会上过一眼就行）

| 日期 | 内容 |
|---|---|
| 09-10（四） | R2V 批次 / 剩余镜头；剪辑 Agent 粗剪 |
| 09-11（五） | **粗剪确认关口**（第二处强制人工关口）；精剪、字幕、混音 |
| 09-12（六） | 成片技术质检 + 合规自查（逐项留证据）；加 AI 生成标识；创空间部署 |
| 09-13（日） | 创作手记；「一句话生成测试」在创空间跑通；平台发布 |
| 09-14（一） | **提交**（22:00 截止）。留足缓冲，不要卡点 |

---

## 四、分工

三人对七个 Agent，按**交接面**切而不是按"模块"切 —— 这样每个人手里是一段连续的管线，边界正好是契约文件，不会出现两人改同一个 Agent。

### A · 总导演—— 创意线 + 全部人工关口

- owns：**编剧 Agent**、**剪辑 Agent**
- 产出契约：`c01_brief`、`c02_screenplay`、`c07_edit_decision`
- 独占职责：三处人工关口的裁决（片约批准、剧本确认、粗剪确认）、质检标准定义、合规自查、创作手记、最终提交
- 仓库 owner
- **09.08 闸门由你拍板**

> 为什么人工关口必须集中在一个人手里：关口是"阻塞等待"，如果三个人都能批，就等于没有关口。

### B · 生成与复现 —— 最重的工程活

- owns：**生成 Agent**、**重试 Agent**
- 产出契约：`c04_gen_request`（消费）、`c05_gen_result`（产出）
- 独占职责：ComfyUI API 接入、GPU 排队与显存管理、`meta.json` 与环境快照、素材上传、仓库工程化（`.gitignore`、CI、分支保护）
- 关键交付：Day 1 的三份冒烟测试 + 真实耗时数字

### C · 中间层 + 部署

- owns：**分镜 Agent**、**提示词 Agent**、**质检 Agent**
- 产出契约：`c03_shotlist`、`c04_gen_request`（产出）、`c06_qc_report`
- 独占职责：视觉一致性资产与 R2V 参考集、GPU 排班表与任务登记表维护
- 09-10 之后：**魔搭创空间部署**

### 会上要对齐的一件事

两位组员已经用 Qoder CLI 搭了 Agent 骨架，但**我不清楚谁手上有哪些**。会上第一件事就是摊开：现有的每个骨架对应上面哪个角色、跟契约差多少。差距大的不要改，直接按契约重写更快 —— 骨架是提示词生成的，重写成本低于对齐成本。

### 契约先于实现，这是三人能真并行的唯一原因

冻结之后：
- B 不需要等 C 的提示词 Agent 写完 —— 直接抄 `examples.json` 里的 `c04_gen_request` 当假输入，就能开发和测生成 Agent
- C 不需要等 B 的生成 Agent —— 拿 `examples.json` 里的 `c05_gen_result` 当假输出，就能开发质检 Agent
- A 不需要等任何人 —— 片约和剧本是源头

**代价是：契约一旦冻结，改动必须全组三人同意。** 因为三个人都在依赖它，单方面改一行就会让另两人的代码全崩。这条要写进仓库 README。

---

## 五、七道接口契约

已经写好并测过了，位置 `contracts/`。这里是概览，细节看文件里的 `description` —— 每个字段的说明都写了**为什么**要这个字段，不只是它叫什么。

| # | 契约名 | 交接面 | 关键字段 | 人工关口 |
|---|---|---|---|---|
| ① | `c01_brief` | 人 → 编剧 | logline、主题、目标时长、画幅、视觉/声音风格、人物形象 | 片约批准 |
| ② | `c02_screenplay` | 编剧 → 分镜 | 场次（含地点/时间/人物/beats）、情绪曲线、台词语言 | **★ 剧本确认（强制阻塞）** |
| ③ | `c03_shotlist` | 分镜 → 提示词 | 镜头清单：镜号/秒数/画幅/景别/运镜/画面描述/声音描述/**工作流类型**、GPU 分批计划 | — |
| ④ | `c04_gen_request` | 提示词 → 生成 | prompt（英文+时间码+`<Picture N>`）、seed、时长、画幅、megapixels、输出前缀、**节点 ID 映射**、素材引用 | — |
| ⑤ | `c05_gen_result` | 生成 → 质检 | 产物路径+sha256、**ffprobe 元数据**、耗时、**实跑参数快照**、模型文件清单 | — |
| ⑥ | `c06_qc_report` | 质检 → 重试/剪辑 | verdict、逐项 checks、失败归因、**可执行的修改建议**、route_to | — |
| ⑦ | `c07_edit_decision` | 剪辑 → 成片 | 时间线、混音、成片元数据、**合规自查+证据路径**、提交物链接 | **★ 粗剪确认（强制阻塞）** |

### 几个刻意的设计，会上值得说一句

**每份产物都带信封**：`schema_version` / `artifact_id` / `contract` / `created_at` / `producer` / `upstream_refs`。`upstream_refs` 串起来就是完整的可追溯链 —— 成片里任何一个镜头，都能反查到它的提示词、种子、分镜、剧本、片约。这是「可观测性 6%」和「复现能力 10 分」的直接材料。

**人工关口是数据，不是约定**。`gate.required = true` 且 `status != approved` 时，校验器会**直接判不通过**，下游 Agent 拿不到放行的产物。全片只有两处强制关口（剧本确认、粗剪确认），其余标 `not_required`。这样"人机边界"就不只是文档上的一句话，而是机器会拦的东西。

**节点 ID 不许硬编码**。`c04` 里的 `node_ids` 要求填 `{语义: [节点ID, 键名]}`，并带上映射表来源。原因是 T2V/I2V 的节点 ID 是子图摊平后的**复合编号**（形如 `140:131`），代码里写 `workflow["131"]` 是取不到的 —— 这个坑我实测踩过。

**`c04` 带条件校验**：声明 I2V 却没给首帧图、声明 R2V 但参考图为空、声明 T2V 却带了素材，都会直接报错。这三种错原本要等排到 GPU 上才暴露，白烧一轮。

**质检建议必须可执行**。`c06.suggested_change` 要求给出 `action`（枚举：换种子/改提示词/改时长/换素材/换工作流/提分辨率/开turbo/转人工）加一个 `patch`（具体覆盖哪些字段）。不允许只写"质量不好"。并且有 `retry_count`/`max_retries`，超过上限强制转人工 —— 防止无限重试烧 GPU。

### 怎么用（三条命令）

```bash
pip install jsonschema

# 列出 7 道契约
python contracts/validate_contract.py --list

# 自检：确认 7 份示例都符合契约结构
python contracts/validate_contract.py --selftest

# 交接前查自己产出的文件（会连人工关口一起查）
python contracts/validate_contract.py --contract c04_gen_request --file artifacts/genreq.S001_c01.json

# 批量查一个目录，自动按 envelope.contract 认类型
python contracts/validate_contract.py --dir artifacts/
```

退出码 0 = 通过，1 = 有不通过，可以直接挂进 git pre-commit 或 CI。

**我已经验证过它不是摆设**：`python contracts/negative_test.py` 会故意造 8 种真实会犯的错（画幅漏括号后缀、时长超 15 秒、沿用默认输出前缀、种子写成字符串、映射表缺 prompt、声明 I2V 没给首帧、声明 R2V 参考图为空、剧本没批准就想往下走），**实测 8/8 全部被抓住**。报告在 `contracts/negative_test_report.txt`。

---

## 六、会上必须定下来的 10 件事

按这个顺序过，每件给个结论就走，不要展开讨论。带 ⚠️ 的是不定就会堵住的。

| # | 要定什么 | 我的建议 |
|---|---|---|
| 1 ⚠️ | **选题 logline** | 按官方建议的「一个人 + 一个空间 + 一件小事」。本地模型擅长氛围/特写/空镜/声音叙事，不擅长复杂打斗和多人对口型 —— 顺着能力边界选题，不要逆着来 |
| 2 ⚠️ | **契约 v1.0 冻结** | 冻结，打 tag。之后改动需三人同意 |
| 3 ⚠️ | **三人分工认领** | 按上面 A/B/C，先摊开现有骨架归属再确认 |
| 4 ⚠️ | **GPU 排班表** | 按工作流类型分批：FL2VA 批次（T2V+I2V）连跑占大头，Ref2VA 批次（R2V）单独一个时段。Day 1 冒烟测试出耗时后再细化 |
| 5 | **每镜头候选数** | 3 个，靠换种子。等 Day 1 的真实耗时出来再确认扛不扛得住 |
| 6 | **创空间怎么连 GPU** | 见下面单独一节，**这是目前最大的未解风险** |
| 7 | **仓库与创空间的账号归属** | 仓库用 A 的账号、两人加 collaborator。**创空间必须建在最终提交作品的那个账号下**，否则提交链接和作品归属对不上 |
| 8 | **09.08 闸门谁拍板** | A，且必须书面记录判定证据、结论、分工调整 |
| 9 ⚠️ | **答疑群提问清单，谁去问** | 见下 |
| 10 | **每日同步时间** | 15 分钟，固定时间。只讲三句：昨天产出哪个契约文件 / 今天计划 / 阻塞项 |

### 第 6 项展开：创空间怎么连到 GPU（最大的未解风险）

**问题**：创空间跑在魔搭的云上，那里**没有我们的 DGX Spark，也没有那 63GB 权重**。所以创空间里的「一句话生成测试」只有三条路：

| 方案 | 做法 | 代价 |
|---|---|---|
| **A** | 创空间当薄前端，HTTP 回调 Spark 上的 ComfyUI | ComfyUI 无任何鉴权，必须套一层带 token 的反向代理，且只在评审期开 |
| **B** | 创空间只跑 Agent 编排 + 展示预生成结果 | 最稳，但「一句话生成」是回放的。掉分不多：自动化程度只占 4%、稳定性 3%，而架构占 30 分 |
| **C** | 创空间自己租 GPU 拉权重 | 63GB + 是 ARM64 专用的 int8 量化权重，x86 GPU 上未必跑得起来。10 天内风险最大，**不建议** |

**建议：按 A 做，把 B 做成兜底。** 也就是创空间的界面设计成「连得上 Spark 就真生成，连不上就播放预生成结果并在界面上明确标注」。这样评审当天网络出问题也不会当场翻车。

**这个决定要在 09.08 闸门之前定**，因为它决定创空间那份代码写什么。负责人 C，Day 3 前给出结论。

### 第 9 项展开：答疑群必须问清的

1. **团队参赛规则** —— 报名表单是单人字段，但免责声明写「本人/**本团队**」，公告里没有任何团队说明。**未确认则可能三人做完只有一人的成绩算数**。要问：是否每人各自报名、以谁名义提交、奖金与 Credits 如何归属
2. **云节点资格与期限** —— 节点能用到什么时候，评审期（09-15→09-18）还开着吗？这直接决定创空间方案 A 可不可行
3. **国际版 Qoder 注册** —— 三人都要注册，报名表里的 Qoder 邮箱须为国际版账号，否则 Credits 静默不发放

> 这三条今晚就该发出去，不要等会议结束。官方回复有延迟，越早问越好。

---

## 七、风险清单

按"会不会让整条线停摆"排序。

| 风险 | 影响 | 缓解 |
|---|---|---|
| **Agent 到 09.08 还跑不通端到端** | 双赛道策略崩 | 闸门预案已写好：转手工保主赛道，Agent 降级为文档仍提交。**Day 1 的冒烟测试就是最早的预警信号** |
| **创空间连不上 GPU** | Agent 赛道提交项缺失 | 方案 A/B 双轨，B 兜底（见上） |
| **团队规则未确认** | 三人做完只有一人算数 | 今晚问答疑群，Day 2 前要有回复。没回复就按最坏情况准备：以 A 名义提交，内部另行约定 |
| **GPU 时间不够** | 镜头数或候选数被砍 | Day 1 就测出真实耗时并算总预算；开 turbo LoRA（2.5×/5×）；砍候选数而不是砍镜头数 |
| **选题超出模型能力** | 反复重生成，时间全烧在试错上 | 选题时明确避开复杂打斗、多人对口型；顺着氛围/特写/空镜/声音叙事走 |
| **风格不一致** | 「自主作品质量 10 分」直接受损 | `c03` 的 `consistency_group` + R2V 参考集复用 + Day 2 就把主角定妆图做出来 |
| **分辨率只有 480p** | 「视听完成度」受损 | 现在三份模板 `megapixels=0.4`（16:9 约 848×480），**成片批次必须提到 1.0**（约 1344×768）才是标称 768p。试错阶段保持 0.4 换速度 |
| **产物互相覆盖** | 白干 | `filename_prefix` 按镜头独立（契约已强制 `shots/S001_c01` 格式，用模板默认值会报错）+ 任务登记表 |
| **meta.json 事后补** | 复现能力与手记都拿不到分 | **从第一个镜头就开始记**。这份数据同时是三样得分材料：复现开源部署 10 分、创作手记质量、可观测性 6%。事后补不回来 |
| **契约被单方面改动** | 三人代码合不起来 | 冻结 + 改动需全组同意 + 校验器进 CI |

---

## 八、建议的会议议程（60 分钟）

| 时间 | 内容 |
|---|---|
| 0–5 min | 念第一节「现状对齐」，让大家知道哪些不用再讨论 |
| 5–10 min | 念第二节「硬约束」，特别是 GPU 分批那条 |
| 10–25 min | **定选题**（清单第 1 项）。这是最容易超时的一段，卡住 15 分钟就投票 |
| 25–35 min | 过契约：只讲第五节那张表 + 「人工关口是数据不是约定」这一条。细节让大家会后自己看 `examples.json` |
| 35–45 min | 认领分工（清单第 3 项）+ 摊开现有骨架归属 |
| 45–55 min | 过清单第 4–10 项，每项给结论 |
| 55–60 min | 复述 Day 1 每个人要交出哪个文件，确认答疑群提问由谁发 |

**会议产出**：这份文档补上 10 个结论，改名 `docs/decisions/2026-09-04-kickoff.md` 进仓库。它同时是创作手记里「团队与流程」章节的素材。

---

*本文档基于 2026-09-04 对 DGX Spark 节点的实测数据写成。工作流、映射表、枚举取值均来自节点上 `/api/object_info` 与 `/userdata` 的真实返回，不是推测。*

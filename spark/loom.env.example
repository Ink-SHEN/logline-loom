# LOOM · Spark 侧配置（部署到 ~/loom/loom.env，权限 600，**不要进仓库**）
#
# LOOM_PROXY_TOKEN：代理鉴权 token。创空间 secrets 里的 LOOM_PROXY_TOKEN 必须与它完全一致，
#                   否则代理一律返回 401。留空时代理拒绝启动——不设 token 等于把 GPU 开放给公网。
LOOM_PROXY_TOKEN=
# ComfyUI 的真实地址。它只监听 127.0.0.1，公网碰不到，只能经代理转发。
LOOM_COMFY_UPSTREAM=http://127.0.0.1:8288
# 代理监听端口 = natapp 隧道的转发目标，改这里不用动 natapp 官网配置。
LOOM_PROXY_PORT=8188
# 创空间的 API 地址（在创空间页面/API 里能查到，形如 https://studio-<owner>-<name>.api-inference.modelscope.net）
LOOM_SPACE_API=https://studio-incion-loom.api-inference.modelscope.net
# 自动上报要用的魔搭 token（网关要求鉴权）。
# 留空 = 不自动上报，地址仍会写进 ~/loom/tunnel_url，手动填进创空间 secrets 效果一样。
# ⚠️ 这个 token 权限较大（能推代码/改 secrets）；竞赛结束归还机器前请务必清空本文件。
LOOM_MS_TOKEN=
# 创空间页面地址（仅用于状态展示，不参与上报）
LOOM_SPACE_URL=https://www.modelscope.cn/studios/Incion/LOOM
